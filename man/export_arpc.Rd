% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_figure.R
\name{export_arpc}
\alias{export_arpc}
\title{Export ggplot2 Figures with Consistent Formatting}
\usage{
export_arpc(
  plot,
  filename,
  path = ".",
  format = c("png", "pdf", "svg", "eps"),
  width = 8,
  height = 6,
  dpi = 300,
  units = "in",
  bg = "white",
  device_args = list()
)
}
\arguments{
\item{plot}{A ggplot2 plot object to export}

\item{filename}{Character string specifying the output filename (without extension)}

\item{path}{Character string specifying the directory path (default: current directory)}

\item{format}{Character string specifying output format: "png", "pdf", "svg", or "eps" (default: "png")}

\item{width}{Numeric value for plot width in inches (default: 8)}

\item{height}{Numeric value for plot height in inches (default: 6)}

\item{dpi}{Numeric value for resolution in dots per inch (default: 300)}

\item{units}{Character string for units: "in", "cm", or "mm" (default: "in")}

\item{bg}{Background color (default: "white")}

\item{device_args}{Named list of additional arguments to pass to the graphics device}
}
\value{
Invisibly returns the full path to the saved file
}
\description{
This function provides a standardized way to export ggplot2 figures with
consistent dimensions, resolution, and formatting options suitable for
ARPC publications and presentations.
}
\examples{
library(ggplot2)
p <- ggplot(mtcars, aes(x = wt, y = mpg)) +
  geom_point() +
  theme_arpc()

# Export as PNG with default settings
export_arpc(p, "my_plot")

# Export as PDF with custom dimensions
export_arpc(p, "my_plot", format = "pdf", width = 10, height = 8)
}
